{
  "name": "decentraphon-presentation",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "slidev --open",
    "dev:pitch": "slidev pitch.md --open --port 3031",
    "build": "slidev build && slidev build pitch.md --base /pitch/ --out dist/pitch",
    "build:full": "slidev build",
    "build:pitch": "slidev build pitch.md --base /pitch/ --out dist/pitch",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.50.0",
    "@slidev/theme-default": "^0.25.0",
    "vue": "^3.5.0"
  }
}
